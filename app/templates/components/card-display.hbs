<a class="card__link" href="#" {{action "openModal"}}>
  <h3 class="card__title">{{card.title}}</h3>
</a>

{{modal-display card=card modalOpen=modalOpen onConfirm=(action "deleteThisCard") onSubmit=(action "cardSubmit") list=list}}